List Guardrails
Return a flat list of guardrails owned by the caller. Pass `legacy=false` — recommended for all new integrations. For AI agents, use https://docs.tavus.io/openapi.yaml for the full HTTP API contract.
