List Voices
Returns available stock `voice_name` values and their linked replica metadata. When you Create Replica with `train_image_url` (image-to-replica), `voice_name` is required—use this list to pick a valid slug and to preview options. For AI agents, use https://docs.tavus.io/openapi.yaml for the full HTTP API contract.
