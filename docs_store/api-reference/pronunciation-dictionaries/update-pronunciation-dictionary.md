Update Pronunciation Dictionary
Update a pronunciation dictionary's name or rules using JSON Patch format (RFC 6902). Supported mutable fields are `name` and `rules`. For AI agents, use https://docs.tavus.io/openapi.yaml for the full HTTP API contract.
