import React from "react";
import { createRoot } from "react-dom/client";
import TavusExperienceBuilder from "./TavusExperienceBuilder.jsx";

createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <TavusExperienceBuilder />
  </React.StrictMode>
);
