{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "framework": null,
  "installCommand": "npm install && cd builder && npm ci",
  "buildCommand": "cd builder && npm run build",
  "outputDirectory": "builder/dist",
  "functions": {
    "api/generate-persona.js": {
      "maxDuration": 60
    },
    "api/brand-theme.js": {
      "maxDuration": 60
    }
  }
}
